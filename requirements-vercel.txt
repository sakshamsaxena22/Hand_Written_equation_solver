# Production Dependencies for Vercel Deployment
# This file contains only the minimal dependencies needed for the serverless API
# For local development with ML models, use requirements.txt

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Image Processing (headless version for serverless)
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# Core Computation
numpy>=1.24.0
sympy>=1.12

# Optional: For enhanced equation parsing
pyparsing>=3.1.0

